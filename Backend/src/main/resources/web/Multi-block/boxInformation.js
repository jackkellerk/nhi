// This is an OOP object to hold the information for the pixel calculations
function boxInformation(x_position, y_position, width, height)
{
    this.x_position = x_position;
    this.y_position = y_position;
    this.width = width;
    this.height = height;
}

